# Example .env for SecretariaJuliana (do not commit real secrets)

# Terapee UI (if you use UI automation)
# TERAPEE_BASE_URL=https://app.terapee.com.br
# TERAPEE_UI_USER=your-user@example.com
# TERAPEE_UI_PASS=your-password
# TERAPEE_PROFILE_DIR=C:\Users\<you>\AppData\Local\Google\Chrome\User Data\Default

# Z-API (WhatsApp gateway) — set these to enable real message sending
# For many Z-API instances the send URL looks like:
# https://api.z-api.io/instances/<instanceId>/token/<token>/send-message
ZAPI_URL=
# The token used by your Z-API instance
ZAP_TOKEN=
# Optional extra header some providers require
CLIENT_TOKEN=

# Debug flags
DEBUG_ZAPI=0         # set to 1 to enable verbose Z-API logging
DEBUG_WEBHOOK=0      # set to 1 to expose /admin/registrations

# Optional defaults
DEFAULT_PATIENT_PHONE=
TERAPEE_REG_QUESTIONS=
TERAPEE_REG_CONFIRMATION=
